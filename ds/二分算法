//二分值偏大
while(l!=r)
    {
        int mid=(l+r)/2+1;
        if(check(mid))
              l=mid;
        else
              r=mid-1;
}


//二分值偏小
 while(l!=r)
    {
        int mid=(l+r)/2;
        if(check(mid))
              r=mid;
        else
              l=mid+1;
}
